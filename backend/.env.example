# Paste your MongoDB Atlas connection string here. Example format:
# MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.abcd123.mongodb.net/notezilla?retryWrites=true&w=majority"
MONGODB_URI="your-mongodb-atlas-connection-string-here"
# Secret used to sign JWTs
JWT_SECRET="replace_with_a_long_random_string"
# Port the backend will listen on
PORT=5000
